query get_category_parent (
  $page: Int = 1,
  $size: Int = 50,
  $parent_category_uuids: [String!] = [],
  $sort_by: [RecordsSortInput!] = {
      properties: [
          { name: "category_weighting", order: ASC }
      ]
  }
) {
  items: records(
    page: $page
    per_page: $size
    filter: {
      deleted_at: { exists: false }
      table: { value: "modules/insites_ecommerce/category" }
      properties: [
        { name: "parent_category_uuids", value_array: $parent_category_uuids } 
        { name: "status", value: "enabled" }
        { name: "slug", not_value_in: "null" }
      ] 
    }
    sort: $sort_by
  ) {
    total_entries
    total_pages
    current_page
    results {
      id 
      uuid: property(name: "uuid")
      category_name: property(name: "category_name") 
      slug: property(name: "slug")      
    }
  }
}
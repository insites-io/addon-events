query get_user_purchased_tickets (
  $page: Int=1
  $size: Int=50
  $purchased_by_uuid: String!
) {
  items: records(
    page: $page
    per_page: $size
    filter: {
      deleted_at: { exists: false }
      table: { value: "modules/insites_events/event_ticket" }
      properties: [
        { name: "purchased_by_uuid", value: $purchased_by_uuid }            
      ]             
    } 
  ) {
    total_entries    
    results {
      id
      uuid: property(name: "uuid")
      purchased_by_uuid: property(name: "purchased_by_uuid")
      event_uuid: property(name: "event_uuid")
	 #properties
    }
  }
}
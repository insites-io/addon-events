query generate_sponsors(
    $page: Int = 1
    $size: Int = 100
) {
    items: records(
        page: $page
        per_page: $size
        filter: {
           	table: { value: "modules/insites_events/event_sponsor" }
            deleted_at: { exists: false }         
        }
    ) {
        results {
          	table          
          	properties  
        }
    }
}
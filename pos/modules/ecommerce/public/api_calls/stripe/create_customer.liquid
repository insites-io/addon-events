---
name: create_customer
to: https://api.stripe.com/v1/customers
enabled: true
trigger_condition: true
request_type: POST
callback: >
  {%- assign response_data = response.body | to_hash -%}
  {% if response_data.error %}
    {%- log response_data.error, type: 'modules/ins_template_ecommerce/create_customer' -%}
  {% endif %}
headers: >
  {% if data %}
    {%- include 'modules/insites_ecommerce/shared/api_credentials' -%}
    {
      {%- log context.exports.payments.gateway.secret_key, type: "SECRET KEY" -%}
      "Content-Type": "application/x-www-form-urlencoded",
      "Authorization": "Bearer {{ context.exports.payments.gateway.secret_key }}"
    }
  {% endif %}
---
{%- log data, type: "data" -%}
{%- if data.email -%}email={{ data.email | url_encode }}&{%- endif -%}{%- if data.description -%}description={{ data.description | url_encode }}&{%- endif -%}name={{ data.name | url_encode }}&{%- if data.source -%}source={{ data.source }}{%- endif -%}{%- for data in data.metadata -%}&metdata[{{ data[0] }}]={{ data[1] | url_encode }}{%- endfor -%}

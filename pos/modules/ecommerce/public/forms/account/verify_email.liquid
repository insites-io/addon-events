---
name: verify_email
resource: User
resource_owner: anyone
fields:
  email:
  profiles:
    modules/ins_template_ecommerce/customer:
      properties:
        is_enabled:
          property_options:
            default: true
        is_activated:
          property_options:
            default: true
---
{% form %}
    <input type="hidden" name="{{ form.fields.profiles['modules/ins_template_ecommerce/customer'].properties.is_enabled.name }}" value="{{ form.fields.profiles['modules/ins_template_ecommerce/customer'].properties.is_enabled.value }}">
    <input type="hidden" name="{{ form.fields.profiles['modules/ins_template_ecommerce/customer'].properties.is_activated.name }}" value="true">
{% endform %}


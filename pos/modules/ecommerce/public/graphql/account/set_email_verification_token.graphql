mutation set_email_verification_token(
  $id: ID!
  $token: String!
) {
  user_update(
    id: $id
    user: {
      profiles: [
        {
          name: "modules/insites_core/insites_default",
          values: {
            properties: [
              { name: "password_token", value: $token }
            ]
          }
        }
      ]
    }
    form_name: "modules/ins_template_ecommerce/set_email_verification_token"
  ) {
    id
  }
}
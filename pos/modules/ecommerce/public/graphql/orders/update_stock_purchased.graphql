mutation update_stock (
  $id: ID!,
  $model_name: String!
  $qty: Float
) {
  items: record_update(
    id: $id
    record: {
      table: $model_name
      properties: { name: "stock", decrement: $qty }
    }
  ) {
    id
    table
    stock: property(name: "stock")
  }
}
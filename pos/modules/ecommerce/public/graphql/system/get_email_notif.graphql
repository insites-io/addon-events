query get_email_notif (
  $name: String!
){
  items: admin_email_notifications (
    filter: {
      name: { contains: $name }
    }
  ) {
    total_entries
    results {
      id
      name
      metadata
    }
  }
}